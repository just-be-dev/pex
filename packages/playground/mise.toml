[tasks.dev]
description = "Run playground in dev mode with hot reload"
run = "bun --hot index.html"

[tasks.build]
description = "Build playground for production"
run = "bun build index.html --outdir dist --minify"

[tasks.clean]
description = "Clean build artifacts"
run = "rm -rf dist"

[tasks.deploy]
description = "Build and deploy playground to Cloudflare Workers"
run = "bunx @just-be/deploy@latest"
depends = ["build"]

pre-commit:
  parallel: true
  commands:
    oxlint:
      glob: "*.{js,ts,jsx,tsx}"
      run: oxlint --fix {staged_files}
      stage_fixed: true

pre-push:
  commands:
    typecheck:
      run: mise run typecheck
